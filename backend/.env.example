# Port (automatically set by cloud providers like Railway, Render, Heroku)
PORT=3002

# Encryption key for credential storage (REQUIRED - generate with: openssl rand -hex 32)
ENCRYPTION_KEY=your-64-character-hex-encryption-key-here

# Frontend URL for CORS (optional - defaults to GitHub Pages)
FRONTEND_URL=https://swipswaps.github.io

